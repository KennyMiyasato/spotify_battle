<section id="two" class="wrapper alt style2">
  <section class="spotlight">
    <div class="image"><img src="<%= @artist_1_pic %>" alt="<%= @artist_1 %>" /></div><div class="content">
      <h2> <%= @artist_1.split.map(&:capitalize).join(' ') %> </h2>
      <p> <%= @clean_1_number %> </p>
    </div>
  </section>
  <section class="spotlight">
    <div class="image"><img src="<%= @artist_2_pic %>" alt="<%= @artist_2 %>" /></div><div class="content">
      <h2> <%= @artist_2.split.map(&:capitalize).join(' ') %> </h2>
      <p> <%= @clean_2_number %> </p>
    </div>
  </section>

<body>

  <div id="header">
    <h1><%= @artist_1.split.map(&:capitalize).join(' ') %>
      vs
      <%= @artist_2.split.map(&:capitalize).join(' ') %>
    </h1>
  </div>

  <div id="artist_1">
    <div class="artist_img">
      <img src="<%= @artist_1_pic %>" alt="<%= @artist_1 %>" />
    </div>
    <h1> <%= @artist_1.split.map(&:capitalize).join(' ') %> </h1>
    <p>
      <%= @clean_1_number %>
    </p>
  </div>

  <div id="artist_2">
    <div class="artist_img">
      <img src="<%= @artist_2_pic %>" alt="<%= @artist_2 %>" />
    </div>
    <h1> <%= @artist_2.split.map(&:capitalize).join(' ') %> </h1>
    <p>
      <%= @clean_2_number %>
    </p>
  </div>

</body>
